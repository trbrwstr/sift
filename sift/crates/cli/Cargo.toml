[package]
name = "sift-cli"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "logforge"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }

logforge-core = { path = "../core" }
logforge-formats = { path = "../formats" }
logforge-output = { path = "../output" }