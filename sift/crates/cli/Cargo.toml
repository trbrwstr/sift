[package]
name = "sift-cli"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "sift"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }

sift-core = { path = "../core" }
sift-formats = { path = "../formats" }
sift-output = { path = "../output" }
